#include <stdio.h>
#include <stdlib.h>

int main() {
	// int b = 10;	
	// int dim2 = 20;
	// int* a = malloc(b * dim2);

	// a[1 * 10] = 10;

	// exit(0);

	char a[10];
	if(a[0] == '\0')
		a[0] = '0';

    return 0;
}
